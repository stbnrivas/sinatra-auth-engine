require 'spec_helper'

describe Sinatra::Auth::Engine do
  it 'has a version number' do
    expect(Sinatra::Auth::Engine::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
